{{top-nav}}

<div class="vote-container">
	<h1>Voting</h1>
	<button {{action 'toggleModal'}}>Cast a vote</button>
	{{vote-table}}

	{{#if showModal}}
		{{vote-modal showModal=showModal}}
	{{/if}}
</div>
