{{#if list}}
	<div class="vote-table">
		<table>
			{{#each list key="@index" as |data|}}
				<tr>
					<td>{{data.name}}</td> <td>{{data.votes}}</td> <td><button {{action 'vote' data}}>+</button></td>
				</tr>
			{{/each}}
		</table>
	</div>
{{/if}}
